<h3 class="box-content">
	유저 창
</h3>
<% if (!isAuthenticated) { %>
	<button onClick="location.href='user/login'" class="btn">
		로그인 하기
	</button>
	<button onClick="location.href='user/signup'" class="btn">
		회원가입 하기
	</button>
<% } else{ %>
	<div>
		<p>
			이름 : <%= currentUser.name %>
			이메일 : <%= currentUser.email ? currentUser.email : "미등록" %>
		</p>
	</div>
	<button onClick="location.href='user/logout'" class="btn">
		로그아웃
	</button>
<% } %>
<button onClick="location.href='/'" class="btn">
	홈으로 가기
</button>